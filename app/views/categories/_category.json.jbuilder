json.extract! category, :id, :name, :order, :day, :time, :duration, :ballrooms, :split, :cost_override, :studio_cost_override, :max_heat_size, :locked, :pro, :routines, :use_category_scoring, :created_at, :updated_at
json.url category_url(category, format: :json)
